import 'package:flutter/material.dart';

class QR_page extends StatelessWidget {
  const QR_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('QR Screen'),),);
  }
}